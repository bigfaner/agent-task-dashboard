---
status: "completed"
started: "2026-05-07 01:16"
completed: "2026-05-07 01:32"
time_spent: "~16m"
---

# Task Record: 2.2 API Handlers — REST Endpoints

## Summary
Implemented all 7 REST API endpoints in internal/handler/api.go: GET /api/projects (list), GET /api/projects/:id (get), GET /api/projects/:id/features (list), GET /api/projects/:id/features/:slug (get), GET /api/projects/:id/features/:slug/tasks (list), GET /api/projects/:id/features/:slug/tasks/:taskId (get), GET /api/projects/:id/features/:slug/dependencies (dependency graph). All endpoints return JSON with meta.lastUpdated. Dependencies endpoint resolves wildcards and marks cross-feature edges. Task detail endpoint calls ParseTaskFile/ParseRecordFile for acceptance criteria and execution records. Also added NewScannerWithFS to scanner package for test injection.

## Changes

### Files Created
- internal/handler/api.go
- internal/handler/api_test.go

### Files Modified
- internal/scanner/scanner.go

### Key Decisions
- completionPct returned as float64 (not string) matching api-handbook.json spec
- estimatedTime always present in task summary (empty string when unset)
- Dependencies endpoint uses scanner's already-expanded wildcard dependencies
- Cross-feature edges detected by building taskFeatureMap across all project features
- NewScannerWithFS added to scanner package for clean test filesystem injection
- findTask uses task.ID matching (not key) per api-handbook spec
- ErrorHandler middleware wraps all API routes for consistent error responses

## Test Results
- **Passed**: 28
- **Failed**: 0
- **Coverage**: 92.0%

## Acceptance Criteria
- [x] internal/handler/api.go implements RegisterAPI(r *gin.Engine, scanner *Scanner)
- [x] GET /api/projects returns array of project summaries with computed stats
- [x] GET /api/projects/:id returns single project with feature list
- [x] GET /api/projects/:id/features returns feature array with task counts
- [x] GET /api/projects/:id/features/:slug returns feature with tasks and phases
- [x] GET /api/projects/:id/features/:slug/tasks returns task array with all fields
- [x] GET /api/projects/:id/features/:slug/tasks/:taskId returns full task with acceptanceCriteria and executionRecord
- [x] GET /api/projects/:id/features/:slug/dependencies returns DependencyGraph with nodes and expanded edges
- [x] All responses include meta.lastUpdated timestamp
- [x] 404 returned for non-existent project/feature/task IDs
- [x] completionPct is 0 when totalTasks is 0 (no division by zero)
- [x] Integration tests using httptest + Gin test mode: all endpoints happy path + 404s

## Notes
28 new API handler tests added to existing 17 middleware tests (45 total in handler package). Scanner package gained NewScannerWithFS for test injection. All 4 packages pass with >80% coverage.
