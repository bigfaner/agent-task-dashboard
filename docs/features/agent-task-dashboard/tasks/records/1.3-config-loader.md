---
status: "completed"
started: "2026-05-07 00:38"
completed: "2026-05-07 00:41"
time_spent: "~3m"
---

# Task Record: 1.3 Config Loader

## Summary
Implemented YAML config file loader with Load(path string) (*Config, error). Config struct has Projects []ProjectConfig and Server ServerConfig matching tech design. ServerConfig.Port defaults to 8080 when omitted. Supports environment variable expansion via os.ExpandEnv and tilde (~) expansion for home directory. Returns ErrConfigInvalid for missing file, malformed YAML, or empty projects list. 9 unit tests with 100% coverage.

## Changes

### Files Created
- internal/config/config.go
- internal/config/config_test.go

### Files Modified
无

### Key Decisions
- Config, ProjectConfig, and ServerConfig structs defined in internal/config package (not internal/model) since they are config-layer types, not shared models
- Tilde expansion uses os.UserHomeDir() with filepath.Join for cross-platform support
- Environment variable expansion applied after tilde expansion to allow $VAR paths to override
- ErrConfigInvalid from internal/model reused for all error cases to stay consistent with existing error types

## Test Results
- **Passed**: 9
- **Failed**: 0
- **Coverage**: 100.0%

## Acceptance Criteria
- [x] internal/config/config.go implements Load(path string) (*Config, error)
- [x] Config struct matches tech design: Projects []ProjectConfig, Server ServerConfig
- [x] ProjectConfig has Name and Path fields with YAML tags
- [x] ServerConfig.Port defaults to 8080 when omitted
- [x] Returns ErrConfigInvalid for missing file or malformed YAML
- [x] Validates that at least one project is defined
- [x] Unit tests: valid config loads, missing file returns error, invalid YAML returns error, default port applied
- [x] All tests pass

## Notes
Config types defined in internal/config package rather than internal/model since they are specific to config loading and not shared across layers. If other layers need them, they can import from config package.
